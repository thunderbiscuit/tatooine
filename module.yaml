product: jvm/app

dependencies:
  # Ktor
  - io.ktor:ktor-server-netty
  - io.ktor:ktor-server-auth
  - io.ktor:ktor-server-config-yaml
  # Logging
  - ch.qos.logback:logback-classic:1.5.21
  # Bitcoin
  - org.bitcoindevkit:bdk-jvm:3.0.0

settings:
  kotlin:
    version: 2.4.0
  ktor:
    enabled: true
    version: 3.5.0
  jvm:
    mainClass: com.coyotebitcoin.tatooine.ApplicationKt
